'use strict';

var fileAssociation = require('./');
var chai = require('chai');
var expect = chai.expect;
var _ = require('lodash');
/*
describe('file-association', function () {

  describe('getAppsThatCanOpenExtension', function() {

    it('return at least one application', function () {
      fileAssociation.getAppsThatCanOpenExtension('.mp3', function (err, apps) {
        expect(!!err).to.be.false;
        expect(apps).to.have.length.above(0);
      });
    });
  });
});*/